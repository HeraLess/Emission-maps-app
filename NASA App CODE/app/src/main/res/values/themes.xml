<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- ========== -->
    <!-- Base Theme -->
    <!-- ========== -->


    <!-- Base application theme. -->
    <style
            name="ThemeApplication"
            parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
        <item name="android:fontFamily">@font/neo_sans_arabic</item>
        <item name="android:popupMenuStyle">@style/PopupMenu</item>
    </style>

    <!-- ========== -->
    <!-- App Themes -->
    <!-- ========== -->

    <style name="ThemeTuketion">
        <!-- Theme Colors -->
        <item name="android:statusBarColor">@color/NavbackColor</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/onPrimary</item>
        <item name="colorPrimaryContainer">@color/primaryContainer</item>
        <item name="colorOnPrimaryContainer">@color/onPrimaryContainer</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/onSecondary</item>
        <item name="colorSecondaryContainer">@color/secondaryContainer</item>
        <item name="colorOnSecondaryContainer">@color/onSecondaryContainer</item>
        <item name="colorTertiary">@color/tertiary</item>
        <item name="colorOnTertiary">@color/onTertiary</item>
        <item name="colorTertiaryContainer">@color/tertiaryContainer</item>
        <item name="colorOnTertiaryContainer">@color/onTertiaryContainer</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorOnBackground">@color/onBackground</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/onSurface</item>
        <item name="colorSurfaceVariant">@color/surfaceVariant</item>
        <item name="colorOnSurfaceVariant">@color/onSurfaceVariant</item>
        <item name="colorOutline">@color/outline</item>
        <item name="colorOnSurfaceInverse">@color/inverseOnSurface</item>
        <item name="colorSurfaceInverse">@color/inverseSurface</item>
        <item name="colorPrimaryInverse">@color/primaryInverse</item>
        <!-- attrs -->
        <item name="NavBackGroundColor">@color/NavbackColor</item>
        <item name="LinearBackGroundColor">@color/LinearBackColor</item>
        <item name="BackItem">@color/BackMyItem</item>
        <item name="ImageSettingColor">@color/switch_color</item>
    </style>

    <!-- ====== -->
    <!-- Splash -->
    <!-- ====== -->


    <!-- Legacy Splash Screen Theme -->
    <style
            name="LegacySplashTheme"
            parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/ic_splash</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <!-- Splash Screen Theme (pre Android 12) -->
    <style
            name="Theme.App.Starting"
            parent="Theme.SplashScreen.IconBackground">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <!-- <item name="windowSplashScreenIconBackgroundColor">@color/yellow6</item> -->
        <item name="postSplashScreenTheme">@style/ThemeApplication</item>
    </style>
    
</resources>